"""Cross-platform local control plane for PostgreSQL, Redis, and Vault.

Use ``python -m infrastructure check`` for read-only prevalidation and
``python -m infrastructure start`` for the complete safe startup workflow.
"""
